apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tools-dig
  name: tools-dig
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tools-dig
  template:
    metadata:
      labels:
        app: tools-dig
    spec:
      containers:
      - image: registry.aliyuncs.com/opsflow/ops:dig
        imagePullPolicy: Always
        name: tools-dig
        ports:
        - containerPort: 80
          protocol: TCP
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
